# Union of the issuer's and backend's dependencies, for the Vercel Function
# entrypoint (api/index.py) which serves either role depending on VERIDICT_ROLE.
# Keep in sync with issuer/requirements.txt and backend/requirements.txt, which
# remain the source of truth for running each service locally.
fastapi>=0.115.0
httpx>=0.27.2
python-multipart>=0.0.10
pydantic>=2.11.0
cbor2>=5.6.4
cryptography>=43.0.1
python-dotenv>=1.0.0
pyjwt[crypto]>=2.9.0
# Proof verification, in-process. No native binary and no node subprocess.
wasmtime
# Postgres driver. DATABASE_URL is injected by the Neon Marketplace integration.
psycopg[binary]
